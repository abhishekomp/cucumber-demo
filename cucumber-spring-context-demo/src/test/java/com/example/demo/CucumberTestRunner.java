package com.example.demo;

import io.cucumber.junit.platform.engine.Cucumber;

@Cucumber
public class CucumberTestRunner {
    // Empty class. JUnit 5 + Cucumber uses this annotation as the entry point.
}
